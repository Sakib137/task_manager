import 'package:flutter/material.dart';

class CirculartProgressIndicatorField extends StatelessWidget {
  const CirculartProgressIndicatorField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
